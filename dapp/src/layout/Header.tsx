import React, { FC } from "react";

const Header: FC = () => {
  return (
    <header className="sticky bg-white inset-0 w-screen h-20 p-8 z-20">
      Header
    </header>
  );
};

export { Header };
